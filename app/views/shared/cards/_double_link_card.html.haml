.card--single.card--media-container.js-card.js-single
  - stack_item.cards.each do |card|
    %article.card.card--image-only.card--gallery
      %a.link--wrapper{href: card.image_href, title: "Image Gallery"}
        %span.card--media__badge
        %figure.card__hero
          = safe_image_tag(card.resized_image_url, class: 'portrait card__image', :alt => "Image Gallery")
        .card__content
          %h2.card__name.accessibility
            Image gallery
          %p
            = card.image_description
