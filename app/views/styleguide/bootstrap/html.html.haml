= render 'styleguide/partials/shared'

.card.styleguide-intro.copy--body.context--content
  .clearfix
    %h1.copy--h1 HTML and Layouts
    %p
      Thoughts. Let's put things here like how to include a basic navbar, etc. Things they wouldn't automatically get from the js or css files, but that are typically included when pulling in Rizzo as a service or application.
    %p
      This should also probably have html for what is typically generated by the UI Components. Although, this could just be integrated into the styleguide as well.
    %p
      <strong>Dev Needs:</strong> We should have the UI components and some basic layouts spit out here as html blocks for users to copy.