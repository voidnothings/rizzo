= render 'styleguide/partials/shared'

- components = YAML.load(File.read(File.expand_path('../../../../data/styleguide/forms_stubs.yml', __FILE__)))

.card.styleguide-intro.styleguide-block.copy--body.context--content
  %h1.copy--h1
    Range Slider

  %p
    This range slider is built upon
    %a{href: "https://github.com/leongersen/noUiSlider"} noUiSlider
    and is used for price and duration filtering within things to do.

  %p
    Without javascript it is replaced by two inputs for min/max


- components['slider_components'].each_with_index do |component, i|
  = sg_component("forms/range_slider", properties: component, count: i, full_width: true)
