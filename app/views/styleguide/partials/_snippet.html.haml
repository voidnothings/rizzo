.styleguide-block__description.card.copy--body.context--content
  %h2.copy--h4 #{properties[:title]}

  - if properties[:description].present?
    - properties[:description].each do |paragraph|
      %p #{paragraph}

  %pre.styleguide.copy--body.language-ruby
    %code #{properties[:snippet]}
