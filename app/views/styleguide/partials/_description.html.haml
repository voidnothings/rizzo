- arguments = opts.values[0]

.styleguide-block__description.card
  .styleguide-block__tabs

    .copy--subtitle.styleguide-block__heading
      = arguments.delete(:styleguide_title)
    .styleguide__partial.copy--body
      \= ui_component('#{component}', properties)

    .copy--body
      Properties

    %pre.styleguide.copy--body.language-ruby<
      %code<
        = JSON.pretty_generate arguments



