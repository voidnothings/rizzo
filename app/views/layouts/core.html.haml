!!! 5
%html
  %head
    = render 'layouts/core/head'
  %body{class: "lp-core #{client_browser(request.user_agent)}" }
    .wrap
      = render 'layouts/core/master_head'
      .content
        = yield
    .js-config.is-hidden   
      = js_configuration        
      = requirejs_include_tag 'app_core' 
      = yield :scripts
