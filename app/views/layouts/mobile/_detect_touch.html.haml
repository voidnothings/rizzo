:javascript
  (function(){
    window.lp = window.lp || {};
    window.lp.touch = true;
    if (('ontouchstart' in window) || window.DocumentTouch && document instanceof DocumentTouch) {
      document.documentElement.className = document.documentElement.className.replace(/\btouch\b/,'no-touch')
      window.lp.touch = false;
    }
  })()